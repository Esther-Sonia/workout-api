{
  "name": "workout-tracker-api",
  "version": "1.0.0",
  "description": "API for workout tracker app",
  "main": "api/index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4",
    "@vercel/node": "^3.0.0"
  }
}